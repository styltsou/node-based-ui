// * Component that renders dotted background on canvas
// The background should be dynamic and move (maybe even scale) when panning the canvas
export default function Background() {
  return <div></div>;
}
